<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/content_frame"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.personal.dillon.butterchurners.MainActivity"
    tools:showIn="@layout/app_bar_main"
    android:background="@android:color/white">

   <ImageView
       android:id="@+id/butterchurners_splash_logo"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:contentDescription="Splash Logo"
       android:src="@mipmap/butterchurners_splash_logo" />

    <TextView
        android:id="@+id/splash_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/butterchurners_splash_logo"
        android:text="Welcome to the CCHL Butterchurners Official Android App!"/>
</RelativeLayout>
